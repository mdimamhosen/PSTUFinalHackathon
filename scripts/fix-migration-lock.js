const fs = require("fs");
fs.writeFileSync(
  "apps/api/prisma/migrations/migration_lock.toml",
  [
    "# Please do not edit this file manually",
    "# It should be added in your version-control system (i.e. Git)",
    'provider = "postgresql"',
    "",
  ].join("\n"),
  { encoding: "utf8" },
);
console.log("lock rewritten");
